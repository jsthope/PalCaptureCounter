-- config.lua
local config = {}

-- By default, the counter will only be shown if the number of captures is less than 10. If you want to always show the counter, set this to true.
config.always_show_count = false

return config